class Empoloyee extends Person {

    constructor(Salary, hireDate) {
        this.Salary  = Salary;
        this.hireDate  = hireDate;
    }
}



    function doJob(Jobtitle) {
        let per = Object.create(person);
        console.log(`${per.name} is a : ${Jobtitle}  who earns : ${this.salary}}`);
    };


